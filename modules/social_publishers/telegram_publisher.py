from __future__ import annotations


def publish_telegram_post(post: dict[str, str]) -> tuple[bool, str]:
    """Placeholder for future Telegram Bot API publishing.

    The semi-automatic distribution workflow only prepares approved copy.
    """
    return False, "Telegram publisher is disabled in this workflow. Use manual posting or the separate scheduled queue later."

