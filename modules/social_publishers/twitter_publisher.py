from __future__ import annotations


def publish_twitter_post(post: dict[str, str]) -> tuple[bool, str]:
    """Placeholder for future X/Twitter publishing.

    Current mode is manual copy-ready only; no API call is made.
    """
    return False, "X/Twitter publisher is disabled. Copy the post manually after approval."
