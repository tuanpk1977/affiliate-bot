from __future__ import annotations


def publish_linkedin_post(post: dict[str, str]) -> tuple[bool, str]:
    """Placeholder for future LinkedIn publishing.

    Current mode is manual copy-ready only; no API call is made.
    """
    return False, "LinkedIn publisher is disabled. Copy the post manually after approval."

