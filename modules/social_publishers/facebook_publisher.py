from __future__ import annotations


def publish_facebook_post(post: dict[str, str]) -> tuple[bool, str]:
    """Placeholder for future Facebook publishing.

    Current mode is manual copy-ready only; no API call is made.
    """
    return False, "Facebook publisher is disabled. Copy the post manually after approval."

